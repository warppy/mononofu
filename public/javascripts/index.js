// Generated by CoffeeScript 1.3.3
var getJst, socket,
  _this = this;

$(function() {
  var $li, $ul;
  $ul = $("ul");
  return $li = $("script.tweets");
});

socket = io.connect();

socket.on("tweets:now", function(data) {
  var html;
  console.log(data);
  data.tweets.created_at = _this.getJst(data.tweets.created_at);
  html = _.template($('script.tweets').html(), {
    data: data.tweets
  });
  return $("ul").prepend(html);
});

getJst = function(utc) {
  var d;
  d = new Date(utc);
  return d.toLocaleString();
};
